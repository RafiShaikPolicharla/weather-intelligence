# Weather Intelligence uses only public Open-Meteo APIs.
# No API keys, Gemini keys, Firebase config, backend URL, or secrets are required.
#
# Keep this file as documentation for evaluators. Do not add private credentials here.
VITE_APP_NAME="Weather Intelligence"
